1:"$Sreact.fragment"
2:I[6166,["177","static/chunks/app/layout-8493e5638b29fe78.js"],"AxeDev"]
3:I[7491,[],""]
4:I[6431,[],""]
6:I[10,[],"OutletBoundary"]
8:I[5791,[],"AsyncMetadataOutlet"]
a:I[10,[],"ViewportBoundary"]
c:I[10,[],"MetadataBoundary"]
d:"$Sreact.suspense"
f:I[457,[],""]
:HL["/_next/static/css/d350892a1aae4366.css","style"]
0:{"P":null,"b":"vEqYcw95b2gYq8WEs8j96","p":"","c":["","docs","giga-primitives",""],"i":false,"f":[[["",{"children":["docs",{"children":[["slug","giga-primitives","d"],{"children":["__PAGE__",{}]}]}]},"$undefined","$undefined",true],["",["$","$1","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/_next/static/css/d350892a1aae4366.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}]],["$","html",null,{"lang":"en","children":[["$","head",null,{"children":[["$","link",null,{"rel":"preconnect","href":"https://fonts.googleapis.com"}],["$","link",null,{"rel":"preconnect","href":"https://fonts.gstatic.com","crossOrigin":""}],["$","link",null,{"rel":"stylesheet","href":"https://fonts.googleapis.com/css2?family=VT323&display=swap"}]]}],["$","body",null,{"className":"min-h-screen bg-giga-navy text-giga-heading antialiased","children":[["$","$L2",null,{}],["$","$L3",null,{"parallelRouterKey":"children","error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L4",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":[[["$","title",null,{"children":"404: This page could not be found."}],["$","div",null,{"style":{"fontFamily":"system-ui,\"Segoe UI\",Roboto,Helvetica,Arial,sans-serif,\"Apple Color Emoji\",\"Segoe UI Emoji\"","height":"100vh","textAlign":"center","display":"flex","flexDirection":"column","alignItems":"center","justifyContent":"center"},"children":["$","div",null,{"children":[["$","style",null,{"dangerouslySetInnerHTML":{"__html":"body{color:#000;background:#fff;margin:0}.next-error-h1{border-right:1px solid rgba(0,0,0,.3)}@media (prefers-color-scheme:dark){body{color:#fff;background:#000}.next-error-h1{border-right:1px solid rgba(255,255,255,.3)}}"}}],["$","h1",null,{"className":"next-error-h1","style":{"display":"inline-block","margin":"0 20px 0 0","padding":"0 23px 0 0","fontSize":24,"fontWeight":500,"verticalAlign":"top","lineHeight":"49px"},"children":404}],["$","div",null,{"style":{"display":"inline-block"},"children":["$","h2",null,{"style":{"fontSize":14,"fontWeight":400,"lineHeight":"49px","margin":0},"children":"This page could not be found."}]}]]}]}]],[]],"forbidden":"$undefined","unauthorized":"$undefined"}]]}]]}]]}],{"children":["docs",["$","$1","c",{"children":[null,["$","$L3",null,{"parallelRouterKey":"children","error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L4",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":"$undefined","forbidden":"$undefined","unauthorized":"$undefined"}]]}],{"children":[["slug","giga-primitives","d"],["$","$1","c",{"children":[null,["$","$L3",null,{"parallelRouterKey":"children","error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L4",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":"$undefined","forbidden":"$undefined","unauthorized":"$undefined"}]]}],{"children":["__PAGE__",["$","$1","c",{"children":["$L5",null,["$","$L6",null,{"children":["$L7",["$","$L8",null,{"promise":"$@9"}]]}]]}],{},null,false]},null,false]},null,false]},null,false],["$","$1","h",{"children":[null,[["$","$La",null,{"children":"$Lb"}],null],["$","$Lc",null,{"children":["$","div",null,{"hidden":true,"children":["$","$d",null,{"fallback":null,"children":"$Le"}]}]}]]}],false]],"m":"$undefined","G":["$f",[]],"s":false,"S":true}
b:[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]
7:null
9:{"metadata":[["$","title","0",{"children":"Gigaverse UI"}],["$","meta","1",{"name":"description","content":"shadcn-style component library in the Gigaverse visual language."}]],"error":null,"digest":"$undefined"}
e:"$9:metadata"
10:I[4986,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],""]
11:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"Tabs"]
12:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"TabsList"]
13:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"TabsTrigger"]
14:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"TabsContent"]
15:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"StatBlock"]
16:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"SectionHeader"]
17:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"NumberedSectionHeader"]
18:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"StatusPip"]
19:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"PingPongLoader"]
1a:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"StepProgress"]
1b:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"StepProgressBar"]
1c:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"LeftBorderAccent"]
1d:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"FactionBadge"]
1e:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"FactionJoinButton"]
1f:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"RarityTag"]
20:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"EquipmentStrip"]
21:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"FactionPopulationCounter"]
22:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"AccentDivider"]
23:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"ShimmerButton"]
24:I[5121,["986","static/chunks/986-7d7647d16d824ffd.js","405","static/chunks/405-39c1081c756aeead.js","121","static/chunks/121-94bc2b3af25a32d8.js","508","static/chunks/app/docs/%5Bslug%5D/page-ffa2557164b5e4bd.js"],"CertificationRow"]
5:["$","main",null,{"className":"mx-auto max-w-5xl px-10 py-14","children":[["$","$L10",null,{"href":"/","className":"text-[14px] tracking-[2px] text-giga-muted hover:text-giga-cyan","children":"← BACK"}],["$","p",null,{"className":"mt-4 text-[13px] uppercase tracking-[3px] text-giga-teal","children":"COMPONENT"}],["$","h1",null,{"className":"mb-2 text-[48px] leading-none text-white uppercase","children":"giga-primitives"}],["$","p",null,{"className":"mb-8 max-w-2xl text-[16px] leading-[1.3] text-giga-muted","children":"Gigaverse-native primitives: status pips, stat blocks, faction badges, rarity tags, shimmer button, step progress, etc."}],["$","$L11",null,{"defaultValue":"preview","children":[["$","$L12",null,{"children":[["$","$L13",null,{"value":"preview","children":"PREVIEW"}],["$","$L13",null,{"value":"usage","children":"USAGE"}],["$","$L13",null,{"value":"install","children":"INSTALL"}],["$","$L13",null,{"value":"source","children":"SOURCE"}]]}],["$","$L14",null,{"value":"preview","children":["$","div",null,{"className":"rounded-giga-xl border border-giga-border bg-giga-dark-card/60 p-10","children":["$","div",null,{"className":"space-y-10","children":[["$","div",null,{"className":"flex flex-wrap items-center gap-6","children":[["$","$L15",null,{"value":"80,241","label":"DAILY PLAYERS","sublabel":"+3.2% 24h"}],["$","$L15",null,{"value":"22.1M","label":"DUNGEON RUNS"}],["$","$L15",null,{"value":"1.2M","label":"ITEMS MINTED"}]]}],["$","$L16",null,{"number":2,"subtitle":"FACTIONS","title":"CHOOSE YOUR SIDE","description":"Eight factions, one permanent pick. Each has a primary + light color variant.","accentColor":"#F5C563"}],["$","$L17",null,{"number":1,"title":"NOOBS"}],["$","div",null,{"className":"flex flex-wrap items-center gap-6","children":[["$","span",null,{"className":"inline-flex items-center gap-2 text-[12px] uppercase tracking-[2px] text-giga-gold","children":[["$","$L18",null,{"tone":"gold"}]," WALLET"]}],["$","span",null,{"className":"inline-flex items-center gap-2 text-[12px] uppercase tracking-[2px] text-giga-cyan","children":[["$","$L18",null,{"tone":"cyan"}]," SIGNING"]}],["$","span",null,{"className":"inline-flex items-center gap-2 text-[12px] uppercase tracking-[2px] text-giga-green","children":[["$","$L18",null,{"tone":"green"}]," CONNECTED"]}],["$","span",null,{"className":"inline-flex items-center gap-2 text-[12px] uppercase tracking-[2px] text-giga-muted","children":[["$","$L18",null,{"tone":"muted"}]," IDLE"]}],["$","span",null,{"className":"inline-flex items-center gap-2 text-[12px] uppercase tracking-[2px] text-giga-live","children":[["$","$L18",null,{"tone":"live"}]," IN COMBAT"]}]]}],["$","div",null,{"className":"flex flex-wrap items-center gap-10","children":[["$","$L19",null,{"label":"LOADING DUNGEON"}],["$","$L1a",null,{"current":2,"total":4}]]}],["$","div",null,{"className":"max-w-md","children":["$","$L1b",null,{"current":1,"steps":[{"label":"WALLET"},{"label":"SIGN"},{"label":"CONFIRM"},{"label":"PLAY"}]}]}],["$","$L1c",null,{"accent":"cyan","children":["$","p",null,{"className":"font-m5x7 text-[17px] text-giga-muted","children":"Left-border text accent — a repeating pattern for descriptive blocks. Accent color per section."}]}],["$","div",null,{"className":"flex flex-wrap gap-2","children":[["$","$L1d",null,{"faction":"crusader"}],["$","$L1d",null,{"faction":"overseer"}],["$","$L1d",null,{"faction":"athena","selected":true}],["$","$L1d",null,{"faction":"archon"}],["$","$L1d",null,{"faction":"foxglove"}],["$","$L1d",null,{"faction":"chobo"}],["$","$L1d",null,{"faction":"summoner"}],["$","$L1d",null,{"faction":"gigus"}]]}],["$","div",null,{"className":"flex flex-wrap gap-3","children":[["$","$L1e",null,{"faction":"crusader"}],["$","$L1e",null,{"faction":"overseer"}],["$","$L1e",null,{"faction":"athena"}],["$","$L1e",null,{"faction":"archon"}],["$","$L1e",null,{"faction":"foxglove"}],["$","$L1e",null,{"faction":"chobo"}],["$","$L1e",null,{"faction":"summoner"}],["$","$L1e",null,{"faction":"gigus"}]]}],["$","div",null,{"className":"flex flex-wrap gap-2","children":[["$","$L1f",null,{"rarity":"common"}],["$","$L1f",null,{"rarity":"rare"}],["$","$L1f",null,{"rarity":"epic"}],["$","$L1f",null,{"rarity":"legendary"}],["$","$L1f",null,{"rarity":"giga"}]]}],["$","$L20",null,{"slots":[{"label":"Helm","filled":true,"rarity":"rare"},{"label":"Chest","filled":true,"rarity":"epic"},{"label":"Weapon","filled":true,"rarity":"legendary"},{"label":"Ring","filled":false},{"label":"Boots","filled":true,"rarity":"common"},{"label":"Relic","filled":false}]}],["$","div",null,{"className":"flex flex-wrap gap-3","children":[["$","$L21",null,{"faction":"crusader","count":"12,841","label":"CRUSADER"}],["$","$L21",null,{"faction":"archon","count":"9,102","label":"ARCHON"}],["$","$L21",null,{"faction":"foxglove","count":"7,544","label":"FOXGLOVE"}]]}],["$","$L22",null,{}],["$","$L23",null,{"children":"ENTER THE GIGAVERSE"}],["$","$L24",null,{"items":[{"label":"BUILT ON ABSTRACT"},{"label":"PAYMENTS BY STRIPE"},{"label":"PROOF OF PLAY"}]}]]}]}]}],"$L25","$L26","$L27"]}]]}]
28:T42a,rounded-giga-xl border border-giga-border bg-giga-dark-card/60 p-10 font-m5x7 text-[17px] leading-[1.5] text-giga-heading [&>*]:mb-4 [&_h1]:mt-6 [&_h1]:mb-3 [&_h1]:font-bitcell [&_h1]:text-[24px] [&_h1]:uppercase [&_h1]:tracking-[2px] [&_h1]:text-giga-gold [&_h2]:mt-6 [&_h2]:mb-3 [&_h2]:font-bitcell [&_h2]:text-[18px] [&_h2]:uppercase [&_h2]:tracking-[2px] [&_h2]:text-giga-gold [&_h3]:mt-5 [&_h3]:font-bitcell [&_h3]:uppercase [&_h3]:tracking-[2px] [&_h3]:text-giga-heading [&_code]:font-bitcell [&_code]:text-giga-cyan [&_pre]:rounded-giga [&_pre]:border [&_pre]:border-giga-border [&_pre]:bg-giga-panel/60 [&_pre]:p-4 [&_pre]:overflow-x-auto [&_pre]:text-[14px] [&_pre_code]:text-giga-heading [&_table]:w-full [&_th]:border-b [&_th]:border-giga-border [&_th]:pb-2 [&_th]:text-left [&_th]:font-bitcell [&_th]:uppercase [&_th]:tracking-[1.5px] [&_th]:text-giga-gold [&_td]:border-b [&_td]:border-giga-border/40 [&_td]:py-2 [&_td]:pr-4 [&_ul]:list-disc [&_ul]:pl-6 [&_ol]:list-decimal [&_ol]:pl-6 [&_a]:text-giga-cyan [&_a]:underline-offset-4 hover:[&_a]:underline25:["$","$L14",null,{"value":"usage","children":["$","div",null,{"className":"$28","children":["$","p",null,{"className":"text-giga-muted","children":["No usage notes yet. Add one at"," ",["$","code",null,{"className":"text-giga-cyan","children":["apps/docs/content/components/","giga-primitives",".md"]}],"."]}]}]}]
29:T7c63,{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "giga-primitives",
  "type": "registry:ui",
  "description": "Gigaverse-native primitives: StatusPip, PingPongLoader, FactionBadge, FactionJoinButton, RarityTag, ShimmerButton, AccentDivider, SectionDivider, StepProgress, StepProgressBar, StatBlock, SectionHeader, NumberedSectionHeader, LeftBorderAccent, CertificationRow, FactionPopulationCounter, SnapSection, EquipmentStrip, SectionAmbientPixels, LandingNavbar, LandingFooter.",
  "dependencies": [
    "@gigaverse/tokens",
    "class-variance-authority",
    "clsx",
    "tailwind-merge"
  ],
  "files": [
    {
      "path": "components/ui/giga-primitives.tsx",
      "content": "'use client'\n\nimport * as React from 'react'\nimport { cva, type VariantProps } from 'class-variance-authority'\nimport { cn } from '../lib/cn'\nimport {\n  factions,\n  factionShinyGradient,\n  shade,\n  type FactionName,\n} from '@gigaverse/tokens'\n\n// ---------------------------------------------------------------------------\n// StatusPip — 7x7 colored dot with exact-spec glow shadows\n// ---------------------------------------------------------------------------\n\nexport const statusPipVariants = cva(\n  'inline-block h-[7px] w-[7px] rounded-full shrink-0',\n  {\n    variants: {\n      tone: {\n        gold: 'bg-giga-gold shadow-[0_0_10px_rgba(245,197,99,0.5)]',\n        cyan: 'bg-giga-cyan shadow-[0_0_10px_rgba(2,199,215,0.45)]',\n        green: 'bg-giga-green shadow-[0_0_10px_rgba(61,217,78,0.45)]',\n        muted: 'bg-giga-muted/60',\n        live: 'bg-giga-live shadow-[0_0_10px_rgba(255,122,122,0.5)] animate-giga-pulse',\n        online: 'bg-giga-green shadow-[0_0_10px_rgba(61,217,78,0.45)]',\n        pending: 'bg-giga-gold shadow-[0_0_10px_rgba(245,197,99,0.5)] animate-giga-pulse',\n        idle: 'bg-giga-muted/60',\n      },\n    },\n    defaultVariants: { tone: 'green' },\n  },\n)\n\nexport interface StatusPipProps\n  extends React.HTMLAttributes<HTMLSpanElement>,\n    VariantProps<typeof statusPipVariants> {}\n\nexport const StatusPip = React.forwardRef<HTMLSpanElement, StatusPipProps>(\n  ({ className, tone, ...props }, ref) => (\n    <span ref={ref} className={cn(statusPipVariants({ tone }), className)} {...props} />\n  ),\n)\nStatusPip.displayName = 'StatusPip'\n\n// ---------------------------------------------------------------------------\n// PingPongLoader — 4 dark blocks with a single sliding gold highlight\n// ---------------------------------------------------------------------------\n\nexport interface PingPongLoaderProps extends React.HTMLAttributes<HTMLDivElement> {\n  label?: string\n}\n\nexport const PingPongLoader = ({ className, label, ...props }: PingPongLoaderProps) => (\n  <div\n    role=\"status\"\n    aria-label={label ?? 'Loading'}\n    className={cn('inline-flex flex-col items-start gap-2', className)}\n    {...props}\n  >\n    <div className=\"relative inline-flex items-center gap-1.5\">\n      {[0, 1, 2, 3].map((i) => (\n        <span\n          key={i}\n          className=\"h-3 w-3 rounded-[2px] border border-giga-border-dark bg-giga-dark-card\"\n        />\n      ))}\n      <span\n        className=\"absolute left-0 top-0 h-3 w-3 rounded-[2px] bg-giga-gold shadow-[0_0_10px_rgba(245,197,99,0.5)] animate-giga-pingpong\"\n        aria-hidden=\"true\"\n      />\n    </div>\n    {label && (\n      <span className=\"font-m5x7 text-[13px] text-giga-muted\">{label}</span>\n    )}\n  </div>\n)\n\n// ---------------------------------------------------------------------------\n// FactionBadge — tinted chip, optional selected state (used for selector grids)\n// ---------------------------------------------------------------------------\n\nexport interface FactionBadgeProps extends React.HTMLAttributes<HTMLButtonElement> {\n  faction: FactionName\n  selected?: boolean\n  asButton?: boolean\n}\n\n/**\n * On-color text: a very dark shade of the faction's primary hex (~85% darker).\n * Reads as near-black but retains a warm/cool tint from the faction palette,\n * so each button feels self-consistent instead of generic white/black on paint.\n */\nfunction factionTextColor(name: FactionName): string {\n  return shade(factions[name].primary, -0.85)\n}\n\nexport const FactionBadge = React.forwardRef<HTMLButtonElement, FactionBadgeProps>(\n  ({ className, faction, selected, children, asButton = false, style, ...props }, ref) => {\n    const Comp = asButton ? 'button' : ('span' as const)\n    const { primary } = factions[faction]\n    const text = factionTextColor(faction)\n    return (\n      <Comp\n        ref={ref as never}\n        aria-pressed={asButton ? selected : undefined}\n        className={cn(\n          'relative inline-flex items-center gap-2 overflow-hidden rounded-giga border-2 px-3 py-1.5 font-bitcell text-[13px] uppercase tracking-[2px] shadow-giga-bevel',\n          'transition-all',\n          \"after:content-[''] after:absolute after:inset-0 after:pointer-events-none\",\n          'after:bg-giga-shimmer after:bg-[length:200%_100%] after:animate-giga-shimmer',\n          selected && 'ring-2 ring-offset-2 ring-offset-giga-navy',\n          asButton && 'cursor-pointer hover:brightness-110 focus-visible:outline-none',\n          className,\n        )}\n        style={{\n          background: factionShinyGradient(faction),\n          borderColor: primary,\n          color: text,\n          ...(selected ? { boxShadow: `0 0 20px ${primary}66` } : null),\n          ...style,\n        }}\n        {...(props as object)}\n      >\n        <span className=\"relative z-10\">{children ?? faction.toUpperCase()}</span>\n      </Comp>\n    )\n  },\n)\nFactionBadge.displayName = 'FactionBadge'\n\n// ---------------------------------------------------------------------------\n// FactionJoinButton — gradient CTA with inline faction colors (light→primary)\n// ---------------------------------------------------------------------------\n\nexport interface FactionJoinButtonProps\n  extends React.ButtonHTMLAttributes<HTMLButtonElement> {\n  faction: FactionName\n}\n\nexport const FactionJoinButton = React.forwardRef<HTMLButtonElement, FactionJoinButtonProps>(\n  ({ className, faction, style, children, ...props }, ref) => {\n    const { primary } = factions[faction]\n    const textColor = factionTextColor(faction)\n    return (\n      <button\n        ref={ref}\n        type=\"button\"\n        className={cn(\n          'relative overflow-hidden rounded-giga-md border-2 px-6 py-2 font-bitcell text-[16px] uppercase tracking-[1.5px] font-bold cursor-pointer shadow-giga-bevel',\n          \"after:content-[''] after:absolute after:inset-0 after:pointer-events-none\",\n          'after:bg-giga-shimmer after:bg-[length:200%_100%] after:animate-giga-shimmer',\n          'transition-opacity hover:opacity-90 active:scale-[0.98]',\n          'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-offset-giga-navy',\n          'disabled:opacity-60 disabled:cursor-not-allowed',\n          className,\n        )}\n        style={{\n          background: factionShinyGradient(faction),\n          borderColor: primary,\n          color: textColor,\n          ...style,\n        }}\n        {...props}\n      >\n        <span className=\"relative z-10\">\n          {children ?? `JOIN ${faction.toUpperCase()}`}\n        </span>\n      </button>\n    )\n  },\n)\nFactionJoinButton.displayName = 'FactionJoinButton'\n\n// ---------------------------------------------------------------------------\n// RarityTag — small corner tag for inventory tiles\n// ---------------------------------------------------------------------------\n\ntype Rarity = 'common' | 'uncommon' | 'rare' | 'epic' | 'legendary' | 'relic' | 'giga'\n\nexport interface RarityTagProps extends React.HTMLAttributes<HTMLSpanElement> {\n  rarity: Rarity\n}\n\nconst rarityClass: Record<Rarity, string> = {\n  common: 'text-rarity-common border-rarity-common/60 bg-rarity-common/10',\n  uncommon: 'text-rarity-uncommon border-rarity-uncommon/60 bg-rarity-uncommon/10',\n  rare: 'text-rarity-rare border-rarity-rare/60 bg-rarity-rare/15',\n  epic: 'text-rarity-epic border-rarity-epic/60 bg-rarity-epic/15',\n  legendary: 'text-rarity-legendary border-rarity-legendary/60 bg-rarity-legendary/10',\n  relic: 'text-rarity-relic border-rarity-relic/60 bg-rarity-relic/15',\n  giga: 'text-rarity-giga border-rarity-giga/60 bg-rarity-giga/10',\n}\n\nexport const RarityTag = React.forwardRef<HTMLSpanElement, RarityTagProps>(\n  ({ className, rarity, children, ...props }, ref) => (\n    <span\n      ref={ref}\n      className={cn(\n        'inline-flex items-center rounded-giga-sm border px-1.5 py-0.5 text-[10px] font-bitcell uppercase tracking-[2px]',\n        rarityClass[rarity],\n        className,\n      )}\n      {...props}\n    >\n      {children ?? rarity}\n    </span>\n  ),\n)\nRarityTag.displayName = 'RarityTag'\n\n// ---------------------------------------------------------------------------\n// ShimmerButton — the ONE button that gets the gold shimmer sweep (PlayNowButton)\n// ---------------------------------------------------------------------------\n\nexport const ShimmerButton = React.forwardRef<\n  HTMLButtonElement,\n  React.ButtonHTMLAttributes<HTMLButtonElement>\n>(({ className, children, ...props }, ref) => (\n  <button\n    ref={ref}\n    className={cn(\n      'relative inline-flex h-12 items-center justify-center overflow-hidden rounded-giga-md px-8',\n      'border-2 border-giga-gold bg-giga-gold-grad text-giga-button-text shadow-giga-bevel',\n      'text-[20px] font-bitcell uppercase tracking-[2px] font-bold',\n      \"after:content-[''] after:absolute after:inset-0 after:pointer-events-none\",\n      'after:bg-giga-shimmer after:bg-[length:200%_100%] after:animate-giga-shimmer',\n      'transition-opacity hover:opacity-90 active:scale-[0.98]',\n      'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-giga-gold/40',\n      'focus-visible:ring-offset-2 focus-visible:ring-offset-giga-navy',\n      'disabled:pointer-events-none disabled:opacity-60 disabled:cursor-not-allowed',\n      className,\n    )}\n    {...props}\n  >\n    <span className=\"relative z-10\">{children}</span>\n  </button>\n))\nShimmerButton.displayName = 'ShimmerButton'\n\n// ---------------------------------------------------------------------------\n// AccentDivider — horizontal gold gradient rule\n// ---------------------------------------------------------------------------\n\nexport const AccentDivider = ({\n  className,\n  ...props\n}: React.HTMLAttributes<HTMLDivElement>) => (\n  <div\n    role=\"separator\"\n    className={cn(\n      'h-px w-full bg-[linear-gradient(90deg,transparent_0%,rgba(245,197,99,0.8)_50%,transparent_100%)]',\n      className,\n    )}\n    {...props}\n  />\n)\nAccentDivider.displayName = 'AccentDivider'\n\n// ---------------------------------------------------------------------------\n// SectionDivider — teal gradient rule, centered container (spec: landing rail)\n// ---------------------------------------------------------------------------\n\nexport const SectionDivider = ({\n  className,\n  ...props\n}: React.HTMLAttributes<HTMLDivElement>) => (\n  <div\n    role=\"separator\"\n    className={cn(\n      'mx-auto w-full max-w-[1400px] px-6 sm:px-8 lg:px-10',\n      className,\n    )}\n    {...props}\n  >\n    <div className=\"h-px w-full bg-giga-divider opacity-40\" />\n  </div>\n)\nSectionDivider.displayName = 'SectionDivider'\n\n// ---------------------------------------------------------------------------\n// StepProgress — N-of-M discrete step dots (onboarding header)\n// ---------------------------------------------------------------------------\n\nexport interface StepProgressProps extends React.HTMLAttributes<HTMLDivElement> {\n  current: number\n  total: number\n}\n\nexport const StepProgress = React.forwardRef<HTMLDivElement, StepProgressProps>(\n  ({ className, current, total, ...props }, ref) => (\n    <div\n      ref={ref}\n      className={cn('inline-flex items-center gap-2', className)}\n      role=\"progressbar\"\n      aria-valuenow={current}\n      aria-valuemin={0}\n      aria-valuemax={total}\n      {...props}\n    >\n      {Array.from({ length: total }).map((_, i) => (\n        <span\n          key={i}\n          className={cn(\n            'h-1.5 w-8 rounded-full transition-colors',\n            i < current ? 'bg-giga-gold' : 'bg-giga-border',\n          )}\n        />\n      ))}\n    </div>\n  ),\n)\nStepProgress.displayName = 'StepProgress'\n\n// ---------------------------------------------------------------------------\n// StepProgressBar — login-flow continuous bar with pulsing teal glow\n// ---------------------------------------------------------------------------\n\nexport interface StepProgressBarProps extends React.HTMLAttributes<HTMLDivElement> {\n  steps: { label: string }[]\n  current: number\n}\n\nexport const StepProgressBar = React.forwardRef<HTMLDivElement, StepProgressBarProps>(\n  ({ className, steps, current, ...props }, ref) => {\n    const progress = steps.length <= 1 ? 100 : (current / (steps.length - 1)) * 100\n    return (\n      <div ref={ref} className={cn('w-full', className)} {...props}>\n        <div\n          className=\"relative h-2 w-full rounded-full border border-giga-border\"\n          style={{ background: '#0c1620' }}\n        >\n          <div\n            className=\"h-full rounded-full bg-giga-progress-grad animate-giga-progress-glow transition-[width] duration-500 ease-out\"\n            style={{ width: `${progress}%` }}\n          />\n        </div>\n        <div className=\"mt-2 flex justify-between\">\n          {steps.map((s, i) => {\n            const status = i < current ? 'complete' : i === current ? 'active' : 'pending'\n            return (\n              <span\n                key={s.label + i}\n                className={cn(\n                  'font-bitcell text-[10px] tracking-[1.5px] uppercase',\n                  status === 'complete' && 'text-giga-green-light',\n                  status === 'active' && 'text-giga-cyan',\n                  status === 'pending' && 'text-giga-muted/50',\n                )}\n              >\n                {s.label}\n              </span>\n            )\n          })}\n        </div>\n      </div>\n    )\n  },\n)\nStepProgressBar.displayName = 'StepProgressBar'\n\n// ---------------------------------------------------------------------------\n// StatBlock — big-number + label + optional sublabel\n// ---------------------------------------------------------------------------\n\nexport interface StatBlockProps extends React.HTMLAttributes<HTMLDivElement> {\n  value: string\n  label: string\n  sublabel?: string\n}\n\nexport const StatBlock = React.forwardRef<HTMLDivElement, StatBlockProps>(\n  ({ className, value, label, sublabel, ...props }, ref) => (\n    <div ref={ref} className={cn('flex flex-col gap-1', className)} {...props}>\n      <span className=\"font-bitcell text-[34px] leading-none tracking-wider text-giga-gold md:text-[42px] 2xl:text-[54px]\">\n        {value}\n      </span>\n      <span className=\"font-bitcell text-[13px] uppercase tracking-[2px] text-giga-muted sm:text-[15px] 2xl:text-[19px]\">\n        {label}\n      </span>\n      {sublabel && <span className=\"text-[12px] text-giga-teal\">{sublabel}</span>}\n    </div>\n  ),\n)\nStatBlock.displayName = 'StatBlock'\n\n// ---------------------------------------------------------------------------\n// SectionHeader — [accent-line] [NUMBER — SUBTITLE] [accent-line] + H2 + description\n// ---------------------------------------------------------------------------\n\nexport interface SectionHeaderProps extends React.HTMLAttributes<HTMLDivElement> {\n  number: string | number\n  subtitle: string\n  title: string\n  description?: string\n  accentColor?: string\n  align?: 'left' | 'center'\n}\n\nexport const SectionHeader = React.forwardRef<HTMLDivElement, SectionHeaderProps>(\n  (\n    {\n      className,\n      number,\n      subtitle,\n      title,\n      description,\n      accentColor = '#F5C563',\n      align = 'left',\n      ...props\n    },\n    ref,\n  ) => {\n    const num =\n      typeof number === 'number' ? String(number).padStart(2, '0') : number\n    return (\n      <div\n        ref={ref}\n        className={cn(\n          'mb-2 sm:mb-4 md:mb-8',\n          align === 'center' ? 'text-center' : 'text-left',\n          className,\n        )}\n        {...props}\n      >\n        <div\n          className={cn(\n            'flex items-center gap-3',\n            align === 'center' && 'justify-center',\n          )}\n        >\n          <span\n            className=\"h-[2px] w-10\"\n            style={{ background: accentColor }}\n            aria-hidden=\"true\"\n          />\n          <span\n            className=\"font-bitcell text-[11px] uppercase tracking-[2px] sm:text-[12px] 2xl:text-[15px]\"\n            style={{ color: accentColor }}\n          >\n            {num} — {subtitle}\n          </span>\n          <span\n            className=\"h-[2px] w-10\"\n            style={{ background: accentColor }}\n            aria-hidden=\"true\"\n          />\n        </div>\n        <h2 className=\"mt-2 font-bitcell text-[24px] uppercase leading-[0.9] tracking-[2px] text-giga-heading sm:mt-3 sm:text-[34px] md:text-[42px] md:tracking-[3px] 2xl:text-[54px]\">\n          {title}\n        </h2>\n        {description && (\n          <p\n            className={cn(\n              'mt-2 max-w-[780px] font-m5x7 text-[17px] leading-[1.35] text-giga-muted sm:mt-3 sm:text-[19px] lg:text-[21px] 2xl:text-[26px]',\n              align === 'center' && 'mx-auto',\n            )}\n          >\n            {description}\n          </p>\n        )}\n      </div>\n    )\n  },\n)\nSectionHeader.displayName = 'SectionHeader'\n\n// Back-compat: keep the old flat \"01 — TITLE\" header as an alias.\nexport interface NumberedSectionHeaderProps extends React.HTMLAttributes<HTMLDivElement> {\n  number: string | number\n  title: string\n}\n\nexport const NumberedSectionHeader = React.forwardRef<\n  HTMLDivElement,\n  NumberedSectionHeaderProps\n>(({ className, number, title, ...props }, ref) => (\n  <div ref={ref} className={cn('flex items-baseline gap-3', className)} {...props}>\n    <span className=\"font-bitcell text-[18px] tracking-[3px] text-giga-teal\">\n      {typeof number === 'number' ? String(number).padStart(2, '0') : number}\n    </span>\n    <span className=\"text-giga-muted\">—</span>\n    <h2 className=\"font-bitcell text-[24px] uppercase tracking-[3px] text-giga-heading\">\n      {title}\n    </h2>\n  </div>\n))\nNumberedSectionHeader.displayName = 'NumberedSectionHeader'\n\n// ---------------------------------------------------------------------------\n// LeftBorderAccent — descriptive text block with accent-colored left border\n// ---------------------------------------------------------------------------\n\nexport interface LeftBorderAccentProps extends React.HTMLAttributes<HTMLDivElement> {\n  accent?: 'cyan' | 'gold' | 'sky' | 'teal'\n}\n\nexport const LeftBorderAccent = React.forwardRef<HTMLDivElement, LeftBorderAccentProps>(\n  ({ className, accent = 'cyan', ...props }, ref) => {\n    const border =\n      accent === 'gold'\n        ? 'border-giga-gold'\n        : accent === 'sky'\n          ? 'border-giga-sky'\n          : accent === 'teal'\n            ? 'border-giga-teal'\n            : 'border-giga-cyan'\n    return (\n      <div\n        ref={ref}\n        className={cn('border-l-2 pl-4', border, className)}\n        {...props}\n      />\n    )\n  },\n)\nLeftBorderAccent.displayName = 'LeftBorderAccent'\n\n// ---------------------------------------------------------------------------\n// CertificationRow — row of logo/badge lockups\n// ---------------------------------------------------------------------------\n\nexport interface CertificationRowProps extends React.HTMLAttributes<HTMLDivElement> {\n  items: { label: string; node?: React.ReactNode }[]\n}\n\nexport const CertificationRow = React.forwardRef<HTMLDivElement, CertificationRowProps>(\n  ({ className, items, ...props }, ref) => (\n    <div\n      ref={ref}\n      className={cn(\n        'flex flex-wrap items-center gap-6 rounded-giga border border-giga-border bg-giga-panel/40 px-6 py-4',\n        className,\n      )}\n      {...props}\n    >\n      {items.map((item) => (\n        <div\n          key={item.label}\n          className=\"flex items-center gap-2 font-bitcell text-[12px] uppercase tracking-[3px] text-giga-muted\"\n        >\n          {item.node && <span className=\"text-giga-cyan\">{item.node}</span>}\n          <span>{item.label}</span>\n        </div>\n      ))}\n    </div>\n  ),\n)\nCertificationRow.displayName = 'CertificationRow'\n\n// ---------------------------------------------------------------------------\n// FactionPopulationCounter — small faction-colored bar + count + label\n// ---------------------------------------------------------------------------\n\nexport interface FactionPopulationCounterProps extends React.HTMLAttributes<HTMLDivElement> {\n  faction: FactionName\n  count: number | string\n  label?: string\n}\n\nexport const FactionPopulationCounter = React.forwardRef<\n  HTMLDivElement,\n  FactionPopulationCounterProps\n>(({ className, faction, count, label, ...props }, ref) => (\n  <div\n    ref={ref}\n    className={cn(\n      'inline-flex items-center gap-3 rounded-giga border border-giga-border bg-giga-panel/60 px-3 py-2',\n      className,\n    )}\n    {...props}\n  >\n    <span className={cn('h-6 w-1 rounded-sm', `bg-faction-${faction}`)} />\n    <div className=\"flex flex-col leading-tight\">\n      <span className=\"font-bitcell text-[16px] text-giga-heading\">{count}</span>\n      <span className=\"font-bitcell text-[10px] uppercase tracking-[2px] text-giga-muted\">\n        {label ?? faction}\n      </span>\n    </div>\n  </div>\n))\nFactionPopulationCounter.displayName = 'FactionPopulationCounter'\n\n// ---------------------------------------------------------------------------\n// SnapSection — scroll-snap section wrapper\n// ---------------------------------------------------------------------------\n\nexport const SnapSection = React.forwardRef<HTMLElement, React.HTMLAttributes<HTMLElement>>(\n  ({ className, ...props }, ref) => (\n    <section\n      ref={ref}\n      className={cn('min-h-screen snap-start snap-always', className)}\n      {...props}\n    />\n  ),\n)\nSnapSection.displayName = 'SnapSection'\n\n// ---------------------------------------------------------------------------\n// EquipmentStrip — horizontal 6-slot equipment row\n// ---------------------------------------------------------------------------\n\nexport interface EquipmentStripProps extends React.HTMLAttributes<HTMLDivElement> {\n  slots: { label: string; icon?: React.ReactNode; rarity?: Rarity; filled?: boolean }[]\n}\n\nexport const EquipmentStrip = React.forwardRef<HTMLDivElement, EquipmentStripProps>(\n  ({ className, slots, ...props }, ref) => (\n    <div\n      ref={ref}\n      className={cn(\n        'flex items-center gap-2 rounded-giga-strip border border-giga-border bg-giga-dark-card/80 p-2.5 sm:p-3 xl:p-4',\n        className,\n      )}\n      {...props}\n    >\n      {slots.map((slot, i) => (\n        <div\n          key={`${slot.label}-${i}`}\n          className={cn(\n            'flex h-14 w-14 flex-col items-center justify-center rounded-giga border-2 text-[10px] font-bitcell uppercase tracking-[2px]',\n            slot.filled\n              ? slot.rarity\n                ? rarityClass[slot.rarity]\n                : 'border-giga-gold/60 bg-giga-panel text-giga-gold'\n              : 'border-giga-border bg-giga-panel/40 text-giga-muted',\n          )}\n        >\n          {slot.icon ?? <span>{slot.label.slice(0, 3)}</span>}\n        </div>\n      ))}\n    </div>\n  ),\n)\nEquipmentStrip.displayName = 'EquipmentStrip'\n\n// ---------------------------------------------------------------------------\n// SectionAmbientPixels — animated pixel glint field, absolute-positioned decor\n// ---------------------------------------------------------------------------\n\nexport interface SectionAmbientPixelsProps extends React.HTMLAttributes<HTMLDivElement> {\n  primaryColor: string\n  secondaryColor: string\n}\n\nconst AMBIENT_POSITIONS: { top: string; left: string; size: number; secondary?: boolean }[] = [\n  { top: '12%', left: '8%', size: 7 },\n  { top: '22%', left: '84%', size: 9, secondary: true },\n  { top: '38%', left: '22%', size: 6 },\n  { top: '44%', left: '68%', size: 8 },\n  { top: '55%', left: '12%', size: 7, secondary: true },\n  { top: '62%', left: '92%', size: 5 },\n  { top: '70%', left: '46%', size: 9, secondary: true },\n  { top: '78%', left: '18%', size: 6 },\n  { top: '84%', left: '74%', size: 8 },\n  { top: '28%', left: '54%', size: 5 },\n]\n\nexport const SectionAmbientPixels = React.forwardRef<HTMLDivElement, SectionAmbientPixelsProps>(\n  ({ className, primaryColor, secondaryColor, ...props }, ref) => (\n    <div\n      ref={ref}\n      aria-hidden=\"true\"\n      className={cn(\n        'pointer-events-none absolute inset-0 z-10 overflow-hidden',\n        className,\n      )}\n      {...props}\n    >\n      {AMBIENT_POSITIONS.map((p, i) => {\n        const color = p.secondary ? secondaryColor : primaryColor\n        return (\n          <span\n            key={i}\n            className=\"absolute animate-giga-pulse\"\n            style={{\n              top: p.top,\n              left: p.left,\n              width: `${p.size}px`,\n              height: `${p.size}px`,\n              background: color,\n              boxShadow: `0 0 10px ${color}, 0 0 18px ${color}`,\n              animationDelay: `${(i * 0.7) % 4}s`,\n              animationDuration: `${7 + (i % 4)}s`,\n            }}\n          />\n        )\n      })}\n    </div>\n  ),\n)\nSectionAmbientPixels.displayName = 'SectionAmbientPixels'\n\n// ---------------------------------------------------------------------------\n// ScrollProgressBar — 2px gold bar fixed to top, driven by scroll progress\n// ---------------------------------------------------------------------------\n\nexport interface ScrollProgressBarProps extends React.HTMLAttributes<HTMLDivElement> {\n  /** Scroll container. Defaults to the window's document. */\n  target?: React.RefObject<HTMLElement | null>\n}\n\nexport const ScrollProgressBar = React.forwardRef<HTMLDivElement, ScrollProgressBarProps>(\n  ({ className, target, style, ...props }, ref) => {\n    const [pct, setPct] = React.useState(0)\n\n    React.useEffect(() => {\n      const el = target?.current ?? null\n      const compute = () => {\n        if (el) {\n          const max = el.scrollHeight - el.clientHeight\n          setPct(max > 0 ? (el.scrollTop / max) * 100 : 0)\n          return\n        }\n        const doc = document.documentElement\n        const max = doc.scrollHeight - window.innerHeight\n        setPct(max > 0 ? (window.scrollY / max) * 100 : 0)\n      }\n      compute()\n      const source: EventTarget = el ?? window\n      source.addEventListener('scroll', compute, { passive: true })\n      window.addEventListener('resize', compute)\n      return () => {\n        source.removeEventListener('scroll', compute)\n        window.removeEventListener('resize', compute)\n      }\n    }, [target])\n\n    return (\n      <div\n        ref={ref}\n        role=\"progressbar\"\n        aria-valuenow={Math.round(pct)}\n        aria-valuemin={0}\n        aria-valuemax={100}\n        className={cn('pointer-events-none absolute inset-x-0 top-0 h-[2px]', className)}\n        {...props}\n      >\n        <div\n          className=\"h-full origin-left bg-giga-gold transition-[transform] duration-100 ease-out\"\n          style={{ transform: `scaleX(${pct / 100})`, ...style }}\n        />\n      </div>\n    )\n  },\n)\nScrollProgressBar.displayName = 'ScrollProgressBar'\n\n// ---------------------------------------------------------------------------\n// LandingNavbar — fixed top bar with scroll progress + animated active link\n// ---------------------------------------------------------------------------\n\nexport interface LandingNavbarProps extends React.HTMLAttributes<HTMLElement> {\n  brand?: React.ReactNode\n  links?: { label: string; href: string; active?: boolean }[]\n  actions?: React.ReactNode\n  /** Show the 2px gold scroll progress bar at the very top. */\n  scrollProgress?: boolean\n}\n\nexport const LandingNavbar = React.forwardRef<HTMLElement, LandingNavbarProps>(\n  ({ className, brand, links = [], actions, scrollProgress = true, ...props }, ref) => (\n    <nav\n      ref={ref}\n      className={cn(\n        'fixed left-0 right-0 top-0 z-40 h-[52px] border-b border-giga-border bg-giga-navy/92 backdrop-blur md:h-[56px]',\n        className,\n      )}\n      {...props}\n    >\n      {scrollProgress && <ScrollProgressBar />}\n      <div className=\"mx-auto flex h-full max-w-[1440px] items-center justify-between px-6 sm:px-8 lg:px-10\">\n        <div className=\"flex items-center gap-8\">\n          {brand && (\n            <span className=\"font-bitcell text-[18px] tracking-[2px] text-giga-gold\">\n              {brand}\n            </span>\n          )}\n          <ul className=\"hidden items-center gap-1 lg:flex\">\n            {links.map((l) => (\n              <li key={l.href} className=\"relative\">\n                <a\n                  href={l.href}\n                  className={cn(\n                    'relative inline-block px-3 py-1.5 font-bitcell text-[15px] uppercase tracking-[1.8px] transition-colors xl:text-[16px] 2xl:text-[20px]',\n                    l.active ? 'text-giga-gold' : 'text-giga-muted hover:text-giga-heading',\n                  )}\n                >\n                  {l.active && (\n                    <span\n                      aria-hidden=\"true\"\n                      className=\"absolute inset-0 -z-10 rounded-giga-sm border-b-2 border-giga-gold bg-giga-gold/10 transition-all\"\n                    />\n                  )}\n                  {l.label}\n                </a>\n              </li>\n            ))}\n          </ul>\n        </div>\n        {actions && <div className=\"flex items-center gap-3\">{actions}</div>}\n      </div>\n    </nav>\n  ),\n)\nLandingNavbar.displayName = 'LandingNavbar'\n\n// ---------------------------------------------------------------------------\n// LandingFooter — fixed bottom bar, brand + socials slots\n// ---------------------------------------------------------------------------\n\nexport interface LandingFooterProps extends React.HTMLAttributes<HTMLElement> {\n  left?: React.ReactNode\n  right?: React.ReactNode\n}\n\nexport const LandingFooter = React.forwardRef<HTMLElement, LandingFooterProps>(\n  ({ className, left, right, ...props }, ref) => (\n    <footer\n      ref={ref}\n      className={cn(\n        'fixed inset-x-0 bottom-0 z-30 h-[56px] border-t border-giga-border bg-giga-panel/95 backdrop-blur-sm md:h-[60px]',\n        className,\n      )}\n      {...props}\n    >\n      <div className=\"mx-auto flex h-full max-w-[1440px] items-center justify-between px-6 sm:px-8 lg:px-10\">\n        <div className=\"flex items-center gap-4 font-bitcell text-[14px] uppercase tracking-[1.5px] text-giga-muted md:text-[16px]\">\n          {left}\n        </div>\n        <div className=\"flex items-center gap-4 text-giga-muted\">{right}</div>\n      </div>\n    </footer>\n  ),\n)\nLandingFooter.displayName = 'LandingFooter'\n",
      "type": "registry:ui",
      "target": "components/ui/giga-primitives.tsx"
    }
  ]
}26:["$","$L14",null,{"value":"install","children":["$","div",null,{"className":"rounded-giga-xl border border-giga-border bg-giga-dark-card/60 p-10 space-y-6","children":[["$","div",null,{"children":[["$","h3",null,{"className":"mb-3 font-bitcell text-[14px] uppercase tracking-[2px] text-giga-teal","children":"NPM — locked version"}],["$","pre",null,{"className":"rounded-giga border border-giga-border bg-giga-panel/60 p-4 text-[14px] text-giga-cyan overflow-x-auto","children":["$","code",null,{"children":"pnpm add @gigaverse/ui @gigaverse/tokens"}]}],["$","p",null,{"className":"mt-2 font-m5x7 text-[14px] text-giga-muted","children":["Then ",["$","code",null,{"className":"text-giga-cyan","children":["import ","{ ","GigaPrimitives"," } from '@gigaverse/ui'"]}],"."]}]]}],["$","div",null,{"children":[["$","h3",null,{"className":"mb-3 font-bitcell text-[14px] uppercase tracking-[2px] text-giga-teal","children":"shadcn registry — ownable copy"}],["$","pre",null,{"className":"rounded-giga border border-giga-border bg-giga-panel/60 p-4 text-[14px] text-giga-cyan overflow-x-auto","children":["$","code",null,{"children":"npx shadcn@latest add http://localhost:3333/r/giga-primitives.json"}]}],["$","p",null,{"className":"mt-2 font-m5x7 text-[14px] text-giga-muted","children":["Writes ",["$","code",null,{"className":"text-giga-cyan","children":["components/ui/","giga-primitives",".tsx"]}]," directly into your project so you own and can modify it."]}]]}],["$","div",null,{"children":[["$","h3",null,{"className":"mb-3 font-bitcell text-[14px] uppercase tracking-[2px] text-giga-teal","children":"Registry JSON"}],["$","pre",null,{"className":"rounded-giga border border-giga-border bg-giga-panel/60 p-4 text-[12px] text-giga-muted overflow-x-auto max-h-[280px]","children":["$","code",null,{"children":"$29"}]}]]}]]}]}]
2a:T758d,'use client'

import * as React from 'react'
import { cva, type VariantProps } from 'class-variance-authority'
import { cn } from '../lib/cn'
import {
  factions,
  factionShinyGradient,
  shade,
  type FactionName,
} from '@gigaverse/tokens'

// ---------------------------------------------------------------------------
// StatusPip — 7x7 colored dot with exact-spec glow shadows
// ---------------------------------------------------------------------------

export const statusPipVariants = cva(
  'inline-block h-[7px] w-[7px] rounded-full shrink-0',
  {
    variants: {
      tone: {
        gold: 'bg-giga-gold shadow-[0_0_10px_rgba(245,197,99,0.5)]',
        cyan: 'bg-giga-cyan shadow-[0_0_10px_rgba(2,199,215,0.45)]',
        green: 'bg-giga-green shadow-[0_0_10px_rgba(61,217,78,0.45)]',
        muted: 'bg-giga-muted/60',
        live: 'bg-giga-live shadow-[0_0_10px_rgba(255,122,122,0.5)] animate-giga-pulse',
        online: 'bg-giga-green shadow-[0_0_10px_rgba(61,217,78,0.45)]',
        pending: 'bg-giga-gold shadow-[0_0_10px_rgba(245,197,99,0.5)] animate-giga-pulse',
        idle: 'bg-giga-muted/60',
      },
    },
    defaultVariants: { tone: 'green' },
  },
)

export interface StatusPipProps
  extends React.HTMLAttributes<HTMLSpanElement>,
    VariantProps<typeof statusPipVariants> {}

export const StatusPip = React.forwardRef<HTMLSpanElement, StatusPipProps>(
  ({ className, tone, ...props }, ref) => (
    <span ref={ref} className={cn(statusPipVariants({ tone }), className)} {...props} />
  ),
)
StatusPip.displayName = 'StatusPip'

// ---------------------------------------------------------------------------
// PingPongLoader — 4 dark blocks with a single sliding gold highlight
// ---------------------------------------------------------------------------

export interface PingPongLoaderProps extends React.HTMLAttributes<HTMLDivElement> {
  label?: string
}

export const PingPongLoader = ({ className, label, ...props }: PingPongLoaderProps) => (
  <div
    role="status"
    aria-label={label ?? 'Loading'}
    className={cn('inline-flex flex-col items-start gap-2', className)}
    {...props}
  >
    <div className="relative inline-flex items-center gap-1.5">
      {[0, 1, 2, 3].map((i) => (
        <span
          key={i}
          className="h-3 w-3 rounded-[2px] border border-giga-border-dark bg-giga-dark-card"
        />
      ))}
      <span
        className="absolute left-0 top-0 h-3 w-3 rounded-[2px] bg-giga-gold shadow-[0_0_10px_rgba(245,197,99,0.5)] animate-giga-pingpong"
        aria-hidden="true"
      />
    </div>
    {label && (
      <span className="font-m5x7 text-[13px] text-giga-muted">{label}</span>
    )}
  </div>
)

// ---------------------------------------------------------------------------
// FactionBadge — tinted chip, optional selected state (used for selector grids)
// ---------------------------------------------------------------------------

export interface FactionBadgeProps extends React.HTMLAttributes<HTMLButtonElement> {
  faction: FactionName
  selected?: boolean
  asButton?: boolean
}

/**
 * On-color text: a very dark shade of the faction's primary hex (~85% darker).
 * Reads as near-black but retains a warm/cool tint from the faction palette,
 * so each button feels self-consistent instead of generic white/black on paint.
 */
function factionTextColor(name: FactionName): string {
  return shade(factions[name].primary, -0.85)
}

export const FactionBadge = React.forwardRef<HTMLButtonElement, FactionBadgeProps>(
  ({ className, faction, selected, children, asButton = false, style, ...props }, ref) => {
    const Comp = asButton ? 'button' : ('span' as const)
    const { primary } = factions[faction]
    const text = factionTextColor(faction)
    return (
      <Comp
        ref={ref as never}
        aria-pressed={asButton ? selected : undefined}
        className={cn(
          'relative inline-flex items-center gap-2 overflow-hidden rounded-giga border-2 px-3 py-1.5 font-bitcell text-[13px] uppercase tracking-[2px] shadow-giga-bevel',
          'transition-all',
          "after:content-[''] after:absolute after:inset-0 after:pointer-events-none",
          'after:bg-giga-shimmer after:bg-[length:200%_100%] after:animate-giga-shimmer',
          selected && 'ring-2 ring-offset-2 ring-offset-giga-navy',
          asButton && 'cursor-pointer hover:brightness-110 focus-visible:outline-none',
          className,
        )}
        style={{
          background: factionShinyGradient(faction),
          borderColor: primary,
          color: text,
          ...(selected ? { boxShadow: `0 0 20px ${primary}66` } : null),
          ...style,
        }}
        {...(props as object)}
      >
        <span className="relative z-10">{children ?? faction.toUpperCase()}</span>
      </Comp>
    )
  },
)
FactionBadge.displayName = 'FactionBadge'

// ---------------------------------------------------------------------------
// FactionJoinButton — gradient CTA with inline faction colors (light→primary)
// ---------------------------------------------------------------------------

export interface FactionJoinButtonProps
  extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  faction: FactionName
}

export const FactionJoinButton = React.forwardRef<HTMLButtonElement, FactionJoinButtonProps>(
  ({ className, faction, style, children, ...props }, ref) => {
    const { primary } = factions[faction]
    const textColor = factionTextColor(faction)
    return (
      <button
        ref={ref}
        type="button"
        className={cn(
          'relative overflow-hidden rounded-giga-md border-2 px-6 py-2 font-bitcell text-[16px] uppercase tracking-[1.5px] font-bold cursor-pointer shadow-giga-bevel',
          "after:content-[''] after:absolute after:inset-0 after:pointer-events-none",
          'after:bg-giga-shimmer after:bg-[length:200%_100%] after:animate-giga-shimmer',
          'transition-opacity hover:opacity-90 active:scale-[0.98]',
          'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-offset-giga-navy',
          'disabled:opacity-60 disabled:cursor-not-allowed',
          className,
        )}
        style={{
          background: factionShinyGradient(faction),
          borderColor: primary,
          color: textColor,
          ...style,
        }}
        {...props}
      >
        <span className="relative z-10">
          {children ?? `JOIN ${faction.toUpperCase()}`}
        </span>
      </button>
    )
  },
)
FactionJoinButton.displayName = 'FactionJoinButton'

// ---------------------------------------------------------------------------
// RarityTag — small corner tag for inventory tiles
// ---------------------------------------------------------------------------

type Rarity = 'common' | 'uncommon' | 'rare' | 'epic' | 'legendary' | 'relic' | 'giga'

export interface RarityTagProps extends React.HTMLAttributes<HTMLSpanElement> {
  rarity: Rarity
}

const rarityClass: Record<Rarity, string> = {
  common: 'text-rarity-common border-rarity-common/60 bg-rarity-common/10',
  uncommon: 'text-rarity-uncommon border-rarity-uncommon/60 bg-rarity-uncommon/10',
  rare: 'text-rarity-rare border-rarity-rare/60 bg-rarity-rare/15',
  epic: 'text-rarity-epic border-rarity-epic/60 bg-rarity-epic/15',
  legendary: 'text-rarity-legendary border-rarity-legendary/60 bg-rarity-legendary/10',
  relic: 'text-rarity-relic border-rarity-relic/60 bg-rarity-relic/15',
  giga: 'text-rarity-giga border-rarity-giga/60 bg-rarity-giga/10',
}

export const RarityTag = React.forwardRef<HTMLSpanElement, RarityTagProps>(
  ({ className, rarity, children, ...props }, ref) => (
    <span
      ref={ref}
      className={cn(
        'inline-flex items-center rounded-giga-sm border px-1.5 py-0.5 text-[10px] font-bitcell uppercase tracking-[2px]',
        rarityClass[rarity],
        className,
      )}
      {...props}
    >
      {children ?? rarity}
    </span>
  ),
)
RarityTag.displayName = 'RarityTag'

// ---------------------------------------------------------------------------
// ShimmerButton — the ONE button that gets the gold shimmer sweep (PlayNowButton)
// ---------------------------------------------------------------------------

export const ShimmerButton = React.forwardRef<
  HTMLButtonElement,
  React.ButtonHTMLAttributes<HTMLButtonElement>
>(({ className, children, ...props }, ref) => (
  <button
    ref={ref}
    className={cn(
      'relative inline-flex h-12 items-center justify-center overflow-hidden rounded-giga-md px-8',
      'border-2 border-giga-gold bg-giga-gold-grad text-giga-button-text shadow-giga-bevel',
      'text-[20px] font-bitcell uppercase tracking-[2px] font-bold',
      "after:content-[''] after:absolute after:inset-0 after:pointer-events-none",
      'after:bg-giga-shimmer after:bg-[length:200%_100%] after:animate-giga-shimmer',
      'transition-opacity hover:opacity-90 active:scale-[0.98]',
      'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-giga-gold/40',
      'focus-visible:ring-offset-2 focus-visible:ring-offset-giga-navy',
      'disabled:pointer-events-none disabled:opacity-60 disabled:cursor-not-allowed',
      className,
    )}
    {...props}
  >
    <span className="relative z-10">{children}</span>
  </button>
))
ShimmerButton.displayName = 'ShimmerButton'

// ---------------------------------------------------------------------------
// AccentDivider — horizontal gold gradient rule
// ---------------------------------------------------------------------------

export const AccentDivider = ({
  className,
  ...props
}: React.HTMLAttributes<HTMLDivElement>) => (
  <div
    role="separator"
    className={cn(
      'h-px w-full bg-[linear-gradient(90deg,transparent_0%,rgba(245,197,99,0.8)_50%,transparent_100%)]',
      className,
    )}
    {...props}
  />
)
AccentDivider.displayName = 'AccentDivider'

// ---------------------------------------------------------------------------
// SectionDivider — teal gradient rule, centered container (spec: landing rail)
// ---------------------------------------------------------------------------

export const SectionDivider = ({
  className,
  ...props
}: React.HTMLAttributes<HTMLDivElement>) => (
  <div
    role="separator"
    className={cn(
      'mx-auto w-full max-w-[1400px] px-6 sm:px-8 lg:px-10',
      className,
    )}
    {...props}
  >
    <div className="h-px w-full bg-giga-divider opacity-40" />
  </div>
)
SectionDivider.displayName = 'SectionDivider'

// ---------------------------------------------------------------------------
// StepProgress — N-of-M discrete step dots (onboarding header)
// ---------------------------------------------------------------------------

export interface StepProgressProps extends React.HTMLAttributes<HTMLDivElement> {
  current: number
  total: number
}

export const StepProgress = React.forwardRef<HTMLDivElement, StepProgressProps>(
  ({ className, current, total, ...props }, ref) => (
    <div
      ref={ref}
      className={cn('inline-flex items-center gap-2', className)}
      role="progressbar"
      aria-valuenow={current}
      aria-valuemin={0}
      aria-valuemax={total}
      {...props}
    >
      {Array.from({ length: total }).map((_, i) => (
        <span
          key={i}
          className={cn(
            'h-1.5 w-8 rounded-full transition-colors',
            i < current ? 'bg-giga-gold' : 'bg-giga-border',
          )}
        />
      ))}
    </div>
  ),
)
StepProgress.displayName = 'StepProgress'

// ---------------------------------------------------------------------------
// StepProgressBar — login-flow continuous bar with pulsing teal glow
// ---------------------------------------------------------------------------

export interface StepProgressBarProps extends React.HTMLAttributes<HTMLDivElement> {
  steps: { label: string }[]
  current: number
}

export const StepProgressBar = React.forwardRef<HTMLDivElement, StepProgressBarProps>(
  ({ className, steps, current, ...props }, ref) => {
    const progress = steps.length <= 1 ? 100 : (current / (steps.length - 1)) * 100
    return (
      <div ref={ref} className={cn('w-full', className)} {...props}>
        <div
          className="relative h-2 w-full rounded-full border border-giga-border"
          style={{ background: '#0c1620' }}
        >
          <div
            className="h-full rounded-full bg-giga-progress-grad animate-giga-progress-glow transition-[width] duration-500 ease-out"
            style={{ width: `${progress}%` }}
          />
        </div>
        <div className="mt-2 flex justify-between">
          {steps.map((s, i) => {
            const status = i < current ? 'complete' : i === current ? 'active' : 'pending'
            return (
              <span
                key={s.label + i}
                className={cn(
                  'font-bitcell text-[10px] tracking-[1.5px] uppercase',
                  status === 'complete' && 'text-giga-green-light',
                  status === 'active' && 'text-giga-cyan',
                  status === 'pending' && 'text-giga-muted/50',
                )}
              >
                {s.label}
              </span>
            )
          })}
        </div>
      </div>
    )
  },
)
StepProgressBar.displayName = 'StepProgressBar'

// ---------------------------------------------------------------------------
// StatBlock — big-number + label + optional sublabel
// ---------------------------------------------------------------------------

export interface StatBlockProps extends React.HTMLAttributes<HTMLDivElement> {
  value: string
  label: string
  sublabel?: string
}

export const StatBlock = React.forwardRef<HTMLDivElement, StatBlockProps>(
  ({ className, value, label, sublabel, ...props }, ref) => (
    <div ref={ref} className={cn('flex flex-col gap-1', className)} {...props}>
      <span className="font-bitcell text-[34px] leading-none tracking-wider text-giga-gold md:text-[42px] 2xl:text-[54px]">
        {value}
      </span>
      <span className="font-bitcell text-[13px] uppercase tracking-[2px] text-giga-muted sm:text-[15px] 2xl:text-[19px]">
        {label}
      </span>
      {sublabel && <span className="text-[12px] text-giga-teal">{sublabel}</span>}
    </div>
  ),
)
StatBlock.displayName = 'StatBlock'

// ---------------------------------------------------------------------------
// SectionHeader — [accent-line] [NUMBER — SUBTITLE] [accent-line] + H2 + description
// ---------------------------------------------------------------------------

export interface SectionHeaderProps extends React.HTMLAttributes<HTMLDivElement> {
  number: string | number
  subtitle: string
  title: string
  description?: string
  accentColor?: string
  align?: 'left' | 'center'
}

export const SectionHeader = React.forwardRef<HTMLDivElement, SectionHeaderProps>(
  (
    {
      className,
      number,
      subtitle,
      title,
      description,
      accentColor = '#F5C563',
      align = 'left',
      ...props
    },
    ref,
  ) => {
    const num =
      typeof number === 'number' ? String(number).padStart(2, '0') : number
    return (
      <div
        ref={ref}
        className={cn(
          'mb-2 sm:mb-4 md:mb-8',
          align === 'center' ? 'text-center' : 'text-left',
          className,
        )}
        {...props}
      >
        <div
          className={cn(
            'flex items-center gap-3',
            align === 'center' && 'justify-center',
          )}
        >
          <span
            className="h-[2px] w-10"
            style={{ background: accentColor }}
            aria-hidden="true"
          />
          <span
            className="font-bitcell text-[11px] uppercase tracking-[2px] sm:text-[12px] 2xl:text-[15px]"
            style={{ color: accentColor }}
          >
            {num} — {subtitle}
          </span>
          <span
            className="h-[2px] w-10"
            style={{ background: accentColor }}
            aria-hidden="true"
          />
        </div>
        <h2 className="mt-2 font-bitcell text-[24px] uppercase leading-[0.9] tracking-[2px] text-giga-heading sm:mt-3 sm:text-[34px] md:text-[42px] md:tracking-[3px] 2xl:text-[54px]">
          {title}
        </h2>
        {description && (
          <p
            className={cn(
              'mt-2 max-w-[780px] font-m5x7 text-[17px] leading-[1.35] text-giga-muted sm:mt-3 sm:text-[19px] lg:text-[21px] 2xl:text-[26px]',
              align === 'center' && 'mx-auto',
            )}
          >
            {description}
          </p>
        )}
      </div>
    )
  },
)
SectionHeader.displayName = 'SectionHeader'

// Back-compat: keep the old flat "01 — TITLE" header as an alias.
export interface NumberedSectionHeaderProps extends React.HTMLAttributes<HTMLDivElement> {
  number: string | number
  title: string
}

export const NumberedSectionHeader = React.forwardRef<
  HTMLDivElement,
  NumberedSectionHeaderProps
>(({ className, number, title, ...props }, ref) => (
  <div ref={ref} className={cn('flex items-baseline gap-3', className)} {...props}>
    <span className="font-bitcell text-[18px] tracking-[3px] text-giga-teal">
      {typeof number === 'number' ? String(number).padStart(2, '0') : number}
    </span>
    <span className="text-giga-muted">—</span>
    <h2 className="font-bitcell text-[24px] uppercase tracking-[3px] text-giga-heading">
      {title}
    </h2>
  </div>
))
NumberedSectionHeader.displayName = 'NumberedSectionHeader'

// ---------------------------------------------------------------------------
// LeftBorderAccent — descriptive text block with accent-colored left border
// ---------------------------------------------------------------------------

export interface LeftBorderAccentProps extends React.HTMLAttributes<HTMLDivElement> {
  accent?: 'cyan' | 'gold' | 'sky' | 'teal'
}

export const LeftBorderAccent = React.forwardRef<HTMLDivElement, LeftBorderAccentProps>(
  ({ className, accent = 'cyan', ...props }, ref) => {
    const border =
      accent === 'gold'
        ? 'border-giga-gold'
        : accent === 'sky'
          ? 'border-giga-sky'
          : accent === 'teal'
            ? 'border-giga-teal'
            : 'border-giga-cyan'
    return (
      <div
        ref={ref}
        className={cn('border-l-2 pl-4', border, className)}
        {...props}
      />
    )
  },
)
LeftBorderAccent.displayName = 'LeftBorderAccent'

// ---------------------------------------------------------------------------
// CertificationRow — row of logo/badge lockups
// ---------------------------------------------------------------------------

export interface CertificationRowProps extends React.HTMLAttributes<HTMLDivElement> {
  items: { label: string; node?: React.ReactNode }[]
}

export const CertificationRow = React.forwardRef<HTMLDivElement, CertificationRowProps>(
  ({ className, items, ...props }, ref) => (
    <div
      ref={ref}
      className={cn(
        'flex flex-wrap items-center gap-6 rounded-giga border border-giga-border bg-giga-panel/40 px-6 py-4',
        className,
      )}
      {...props}
    >
      {items.map((item) => (
        <div
          key={item.label}
          className="flex items-center gap-2 font-bitcell text-[12px] uppercase tracking-[3px] text-giga-muted"
        >
          {item.node && <span className="text-giga-cyan">{item.node}</span>}
          <span>{item.label}</span>
        </div>
      ))}
    </div>
  ),
)
CertificationRow.displayName = 'CertificationRow'

// ---------------------------------------------------------------------------
// FactionPopulationCounter — small faction-colored bar + count + label
// ---------------------------------------------------------------------------

export interface FactionPopulationCounterProps extends React.HTMLAttributes<HTMLDivElement> {
  faction: FactionName
  count: number | string
  label?: string
}

export const FactionPopulationCounter = React.forwardRef<
  HTMLDivElement,
  FactionPopulationCounterProps
>(({ className, faction, count, label, ...props }, ref) => (
  <div
    ref={ref}
    className={cn(
      'inline-flex items-center gap-3 rounded-giga border border-giga-border bg-giga-panel/60 px-3 py-2',
      className,
    )}
    {...props}
  >
    <span className={cn('h-6 w-1 rounded-sm', `bg-faction-${faction}`)} />
    <div className="flex flex-col leading-tight">
      <span className="font-bitcell text-[16px] text-giga-heading">{count}</span>
      <span className="font-bitcell text-[10px] uppercase tracking-[2px] text-giga-muted">
        {label ?? faction}
      </span>
    </div>
  </div>
))
FactionPopulationCounter.displayName = 'FactionPopulationCounter'

// ---------------------------------------------------------------------------
// SnapSection — scroll-snap section wrapper
// ---------------------------------------------------------------------------

export const SnapSection = React.forwardRef<HTMLElement, React.HTMLAttributes<HTMLElement>>(
  ({ className, ...props }, ref) => (
    <section
      ref={ref}
      className={cn('min-h-screen snap-start snap-always', className)}
      {...props}
    />
  ),
)
SnapSection.displayName = 'SnapSection'

// ---------------------------------------------------------------------------
// EquipmentStrip — horizontal 6-slot equipment row
// ---------------------------------------------------------------------------

export interface EquipmentStripProps extends React.HTMLAttributes<HTMLDivElement> {
  slots: { label: string; icon?: React.ReactNode; rarity?: Rarity; filled?: boolean }[]
}

export const EquipmentStrip = React.forwardRef<HTMLDivElement, EquipmentStripProps>(
  ({ className, slots, ...props }, ref) => (
    <div
      ref={ref}
      className={cn(
        'flex items-center gap-2 rounded-giga-strip border border-giga-border bg-giga-dark-card/80 p-2.5 sm:p-3 xl:p-4',
        className,
      )}
      {...props}
    >
      {slots.map((slot, i) => (
        <div
          key={`${slot.label}-${i}`}
          className={cn(
            'flex h-14 w-14 flex-col items-center justify-center rounded-giga border-2 text-[10px] font-bitcell uppercase tracking-[2px]',
            slot.filled
              ? slot.rarity
                ? rarityClass[slot.rarity]
                : 'border-giga-gold/60 bg-giga-panel text-giga-gold'
              : 'border-giga-border bg-giga-panel/40 text-giga-muted',
          )}
        >
          {slot.icon ?? <span>{slot.label.slice(0, 3)}</span>}
        </div>
      ))}
    </div>
  ),
)
EquipmentStrip.displayName = 'EquipmentStrip'

// ---------------------------------------------------------------------------
// SectionAmbientPixels — animated pixel glint field, absolute-positioned decor
// ---------------------------------------------------------------------------

export interface SectionAmbientPixelsProps extends React.HTMLAttributes<HTMLDivElement> {
  primaryColor: string
  secondaryColor: string
}

const AMBIENT_POSITIONS: { top: string; left: string; size: number; secondary?: boolean }[] = [
  { top: '12%', left: '8%', size: 7 },
  { top: '22%', left: '84%', size: 9, secondary: true },
  { top: '38%', left: '22%', size: 6 },
  { top: '44%', left: '68%', size: 8 },
  { top: '55%', left: '12%', size: 7, secondary: true },
  { top: '62%', left: '92%', size: 5 },
  { top: '70%', left: '46%', size: 9, secondary: true },
  { top: '78%', left: '18%', size: 6 },
  { top: '84%', left: '74%', size: 8 },
  { top: '28%', left: '54%', size: 5 },
]

export const SectionAmbientPixels = React.forwardRef<HTMLDivElement, SectionAmbientPixelsProps>(
  ({ className, primaryColor, secondaryColor, ...props }, ref) => (
    <div
      ref={ref}
      aria-hidden="true"
      className={cn(
        'pointer-events-none absolute inset-0 z-10 overflow-hidden',
        className,
      )}
      {...props}
    >
      {AMBIENT_POSITIONS.map((p, i) => {
        const color = p.secondary ? secondaryColor : primaryColor
        return (
          <span
            key={i}
            className="absolute animate-giga-pulse"
            style={{
              top: p.top,
              left: p.left,
              width: `${p.size}px`,
              height: `${p.size}px`,
              background: color,
              boxShadow: `0 0 10px ${color}, 0 0 18px ${color}`,
              animationDelay: `${(i * 0.7) % 4}s`,
              animationDuration: `${7 + (i % 4)}s`,
            }}
          />
        )
      })}
    </div>
  ),
)
SectionAmbientPixels.displayName = 'SectionAmbientPixels'

// ---------------------------------------------------------------------------
// ScrollProgressBar — 2px gold bar fixed to top, driven by scroll progress
// ---------------------------------------------------------------------------

export interface ScrollProgressBarProps extends React.HTMLAttributes<HTMLDivElement> {
  /** Scroll container. Defaults to the window's document. */
  target?: React.RefObject<HTMLElement | null>
}

export const ScrollProgressBar = React.forwardRef<HTMLDivElement, ScrollProgressBarProps>(
  ({ className, target, style, ...props }, ref) => {
    const [pct, setPct] = React.useState(0)

    React.useEffect(() => {
      const el = target?.current ?? null
      const compute = () => {
        if (el) {
          const max = el.scrollHeight - el.clientHeight
          setPct(max > 0 ? (el.scrollTop / max) * 100 : 0)
          return
        }
        const doc = document.documentElement
        const max = doc.scrollHeight - window.innerHeight
        setPct(max > 0 ? (window.scrollY / max) * 100 : 0)
      }
      compute()
      const source: EventTarget = el ?? window
      source.addEventListener('scroll', compute, { passive: true })
      window.addEventListener('resize', compute)
      return () => {
        source.removeEventListener('scroll', compute)
        window.removeEventListener('resize', compute)
      }
    }, [target])

    return (
      <div
        ref={ref}
        role="progressbar"
        aria-valuenow={Math.round(pct)}
        aria-valuemin={0}
        aria-valuemax={100}
        className={cn('pointer-events-none absolute inset-x-0 top-0 h-[2px]', className)}
        {...props}
      >
        <div
          className="h-full origin-left bg-giga-gold transition-[transform] duration-100 ease-out"
          style={{ transform: `scaleX(${pct / 100})`, ...style }}
        />
      </div>
    )
  },
)
ScrollProgressBar.displayName = 'ScrollProgressBar'

// ---------------------------------------------------------------------------
// LandingNavbar — fixed top bar with scroll progress + animated active link
// ---------------------------------------------------------------------------

export interface LandingNavbarProps extends React.HTMLAttributes<HTMLElement> {
  brand?: React.ReactNode
  links?: { label: string; href: string; active?: boolean }[]
  actions?: React.ReactNode
  /** Show the 2px gold scroll progress bar at the very top. */
  scrollProgress?: boolean
}

export const LandingNavbar = React.forwardRef<HTMLElement, LandingNavbarProps>(
  ({ className, brand, links = [], actions, scrollProgress = true, ...props }, ref) => (
    <nav
      ref={ref}
      className={cn(
        'fixed left-0 right-0 top-0 z-40 h-[52px] border-b border-giga-border bg-giga-navy/92 backdrop-blur md:h-[56px]',
        className,
      )}
      {...props}
    >
      {scrollProgress && <ScrollProgressBar />}
      <div className="mx-auto flex h-full max-w-[1440px] items-center justify-between px-6 sm:px-8 lg:px-10">
        <div className="flex items-center gap-8">
          {brand && (
            <span className="font-bitcell text-[18px] tracking-[2px] text-giga-gold">
              {brand}
            </span>
          )}
          <ul className="hidden items-center gap-1 lg:flex">
            {links.map((l) => (
              <li key={l.href} className="relative">
                <a
                  href={l.href}
                  className={cn(
                    'relative inline-block px-3 py-1.5 font-bitcell text-[15px] uppercase tracking-[1.8px] transition-colors xl:text-[16px] 2xl:text-[20px]',
                    l.active ? 'text-giga-gold' : 'text-giga-muted hover:text-giga-heading',
                  )}
                >
                  {l.active && (
                    <span
                      aria-hidden="true"
                      className="absolute inset-0 -z-10 rounded-giga-sm border-b-2 border-giga-gold bg-giga-gold/10 transition-all"
                    />
                  )}
                  {l.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
        {actions && <div className="flex items-center gap-3">{actions}</div>}
      </div>
    </nav>
  ),
)
LandingNavbar.displayName = 'LandingNavbar'

// ---------------------------------------------------------------------------
// LandingFooter — fixed bottom bar, brand + socials slots
// ---------------------------------------------------------------------------

export interface LandingFooterProps extends React.HTMLAttributes<HTMLElement> {
  left?: React.ReactNode
  right?: React.ReactNode
}

export const LandingFooter = React.forwardRef<HTMLElement, LandingFooterProps>(
  ({ className, left, right, ...props }, ref) => (
    <footer
      ref={ref}
      className={cn(
        'fixed inset-x-0 bottom-0 z-30 h-[56px] border-t border-giga-border bg-giga-panel/95 backdrop-blur-sm md:h-[60px]',
        className,
      )}
      {...props}
    >
      <div className="mx-auto flex h-full max-w-[1440px] items-center justify-between px-6 sm:px-8 lg:px-10">
        <div className="flex items-center gap-4 font-bitcell text-[14px] uppercase tracking-[1.5px] text-giga-muted md:text-[16px]">
          {left}
        </div>
        <div className="flex items-center gap-4 text-giga-muted">{right}</div>
      </div>
    </footer>
  ),
)
LandingFooter.displayName = 'LandingFooter'
27:["$","$L14",null,{"value":"source","children":["$","div",null,{"className":"rounded-giga-xl border border-giga-border bg-giga-dark-card/60 p-6","children":["$","pre",null,{"className":"overflow-x-auto text-[13px] leading-[1.4]","children":["$","code",null,{"className":"font-bitcell text-giga-heading","children":"$2a"}]}]}]}]
